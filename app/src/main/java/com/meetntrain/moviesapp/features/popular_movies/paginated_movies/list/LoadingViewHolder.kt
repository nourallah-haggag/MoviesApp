package com.meetntrain.moviesapp.features.popular_movies.paginated_movies.list

import android.view.View

class LoadingViewHolder(itemView:View) : BaseViewHolder(itemView) {
    override fun <T> bind(item: T) {
        // nothing to bind here :D
    }
}